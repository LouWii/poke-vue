const defaultLanguage = 1

const getInitialState = () => {
  return {
    defaultLanguage,
    userLanguage: defaultLanguage
  }
}

const state = getInitialState()

const getters = {}

const actions = {
  
}

const mutations = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}